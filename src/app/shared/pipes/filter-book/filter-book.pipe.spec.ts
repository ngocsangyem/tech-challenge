import { FilterBookPipe } from './filter-book.pipe';

describe('FilterBookPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBookPipe();
    expect(pipe).toBeTruthy();
  });
});
